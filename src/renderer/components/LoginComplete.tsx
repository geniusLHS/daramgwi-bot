import React, { useRef, useState, useEffect } from 'react';
import '../App.css';

function DoingComplete() {
  return <div className="LoginComplete">로그인에 성공하였습니다.</div>;
}

export default DoingComplete;
