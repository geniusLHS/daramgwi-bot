import React, { useRef, useState, useEffect } from 'react';
import '../App.css';

function DoingLogin() {
  //  = useRef();

  return <div className="DoingLogin"> 로그인 중입니다... </div>;
}

export default DoingLogin;
